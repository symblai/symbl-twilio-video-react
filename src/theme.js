import { createMuiTheme } from '@material-ui/core';

export default createMuiTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#1A1A1A',
        },
    },
    sidebarWidth: 260,
    sidebarMobileHeight: 90,
});
